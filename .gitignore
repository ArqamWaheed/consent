.env
.venv/
__pycache__/
*.pyc
.streamlit/secrets.toml

# Strategy playbook — local only, never published.
03-PLAYBOOK-consent.md

# Snowsight result downloads land here when the browser save dialog defaults to cwd.
consent_bootstrap_*.csv
.streamlit/

# Chrome partial downloads that land in the repo dir.
*.crdownload

# Key material, in every shape it might land in.
*.p8
*.pem
*.key
secrets.toml
