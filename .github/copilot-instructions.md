See ../AGENTS.md — the same rules apply. Read `memory.md` first, update it last.

Two rules that are easy to break by accident:
1. The app role must never gain SELECT on `RAW_CASES`. That absence is the project.
2. Never write a ✅ into the capability grid that was not observed in a real account.
